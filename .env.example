# ── Local development ─────────────────────────────────────────────────────────

PORT=4000

# Your Google Cloud project ID
GOOGLE_PROJECT_ID=niro-471913

# Vertex AI region (this is where Gemini runs)
GOOGLE_LOCATION=us-central1

# Path to your service account key file (LOCAL ONLY — never commit this file)
# Download from GCP Console → IAM → Service Accounts → your account → Keys
GOOGLE_APPLICATION_CREDENTIALS=./gcp-key.json

# URL of the Smart Report Engine running locally
SMART_REPORT_ENGINE_URL=http://localhost:3000


# ── Render deployment (replace GOOGLE_APPLICATION_CREDENTIALS with this) ──────
#
# On Render, you can't upload a key file. Instead, paste the ENTIRE
# service account JSON as a single string in this env var.
# The app will write it to /tmp at startup automatically.
#
# GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"niro-471913",...}
